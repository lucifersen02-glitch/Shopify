{% schema %}
{
  "name": "About Section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "About Us"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content",
      "default": "<p>At PerfectFinds, we are passionate about curating high-quality products that make a difference in your daily life. Our commitment to excellence and customer satisfaction drives everything we do.</p><p>We carefully select each item in our collection, ensuring that every product meets our high standards for quality, functionality, and value. Whether you're looking for everyday essentials or unique finds, PerfectFinds is your trusted partner in discovering products that truly matter.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    }
  ],
  "presets": [
    {
      "name": "About Section"
    }
  ]
}
{% endschema %}

<section id="about" class="about">
  <div class="container">
    <div class="about__content">
      <div class="about__text">
        <h2 class="section-heading">{{ section.settings.heading }}</h2>
        <div class="about__description">
          {{ section.settings.content }}
        </div>
      </div>
      {% if section.settings.image %}
        <div class="about__image">
          {{ section.settings.image | image_url: width: 600 | image_tag: loading: 'lazy', alt: section.settings.heading }}
        </div>
      {% endif %}
    </div>
  </div>
</section>
